fields:
    php_path:
        label: 'Путь до интерпретатора php'
        span: auto
        default: php
        type: text
        comment: 'php или /bin/php7.4 или ENV чтобы взять из PHP_PATH переменной окружения'
    mail_for_notifications:
        label: 'Адреса email для уведомлений'
        prompt: 'Добавить e-mail'
        span: auto
        type: repeater
        form:
            fields:
                email:
                    label: email
                    span: full
                    type: text
tabs:
    fields:
        db_host:
            label: Host
            span: auto
            default: localhost
            type: text
            tab: 'MySQL ROOT'
        db_port:
            label: Port
            span: auto
            default: '3306'
            type: text
            tab: 'MySQL ROOT'
        db_user:
            label: User
            span: auto
            type: text
            tab: 'MySQL ROOT'
        db_pass:
            label: Password
            span: auto
            type: text
            tab: 'MySQL ROOT'
